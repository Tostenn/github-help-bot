{
  "services": [
    {
      "name": "webhook",
      "sourceDir": "webhook",
      "startCommand": "python server.py",
      "buildCommand": "pip install -r requirements.txt",
      "env": {
        "PORT": "5000"
      }
    },
    {
      "name": "bot",
      "sourceDir": "bot",
      "startCommand": "python bot.py",
      "buildCommand": "pip install -r requirements.txt"
    }
  ],
  "plugins": []
}
